[advisories]
# RUSTSEC-2024-0436 — `paste` crate is unmaintained.
# `paste` is a transitive dependency of `rmcp` used only for macro hygiene;
# no exploit path is known.  Accepted pending an upstream rmcp update that
# drops the dependency.
#
# RUSTSEC-2026-0097 — `rand` 0.8.5 can be unsound with a custom logger that
# calls `rand::rng()`. This project does not install a custom logger, and the
# dependency is transitive through tungstenite/axum's WebSocket stack. Accepted
# pending an upstream axum/tungstenite release that moves off rand 0.8.
ignore = ["RUSTSEC-2024-0436", "RUSTSEC-2026-0097"]
