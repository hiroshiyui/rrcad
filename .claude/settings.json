{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.h|*.cpp) clang-format -i \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "clang-format"
          }
        ]
      }
    ]
  }
}
